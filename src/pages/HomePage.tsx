import { Hero } from '../components/Hero.tsx'

export function HomePage() {
  return (
    <Hero
      eyebrow="Lean by default"
      title="Modern web apps without bloat."
      description="NERO gives teams a focused foundation for shipping quickly, with clean routing and reusable UI primitives."
    />
  )
}
